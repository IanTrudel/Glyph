test_c_layout_f32 u =
  p = {fx: 1.5, fy: 2.5}
  xv = float_to_int(p.fx)
  yv = float_to_int(p.fy)
  assert_eq(xv, 1)
  assert_eq(yv, 2)
  sum = p.fx + p.fy
  assert_eq(float_to_int(sum), 4)
  diff = p.fx - p.fy
  assert_eq(float_to_int(diff), 0 - 1)
  0


test_resolve_fld u =
  reg = [["a", "b", "c"], ["x", "y"]]
  r = resolve_fld_off(reg, "b", ["a", "c"])
  assert_eq(r, 1)
  r2 = resolve_fld_off(reg, "y", ["x"])
  assert_eq(r2, 1)
  r3 = resolve_fld_off(reg, "z", ["q"])
  assert_eq(r3, 0 - 1)
  0

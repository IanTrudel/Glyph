test_str_eq_ctor_payload_self_cmp u =
  src = r"""f o = match o
  Some(c) -> c == c
  _ -> 0"""
  mir = compile_fn_sig(src, "?S -> B")
  c = cg_function(mir)
  assert_eq(str_contains(c, "glyph_str_eq"), 1)
  0

fet_loop name struct_map i =
  match i >= glyph_array_len(struct_map)
    true -> []
    _ ->
      entry = struct_map[i]
      match glyph_str_eq(glyph_arr_get_str(entry, 0), name)
        true -> entry[2]
        _ -> fet_loop(name, struct_map, i + 1)


json_gen pool idx =
  node = pool[idx]
  match node.tag
    0 -> "null"
    1 -> match node.nval
      true -> "true"
      _ -> "false"
    2 -> int_to_str(node.nval)
    3 -> json_gen_str(node.sval)
    4 -> json_gen_arr(pool, node)
    5 -> json_gen_obj(pool, node)
    _ -> "null"

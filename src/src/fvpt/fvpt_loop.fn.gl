fvpt_loop vmap i name =
  match i >= glyph_array_len(vmap)
    true -> []
    _ ->
      entry = vmap[i]
      match glyph_str_eq(entry.ve_name, name)
        true -> entry.ve_ptypes
        _ -> fvpt_loop(vmap, i + 1, name)

stpfn_scan name i len =
  match i >= len
    true -> name
    _ ->
      c = glyph_str_char_at(name, i)
      match c == 60
        true -> glyph_str_slice(name, 0, i)
        _ -> stpfn_scan(name, i + 1, len)

test_tok_extras u =
  assert_eq(tk_else(), 21)
  assert_eq(tk_for(), 23)
  assert_eq(tk_in(), 24)
  assert(tk_error() != 0)
  src = r""""hello {x}""""
  toks = tokenize(src)
  kinds = test_collect_kinds(toks, 0, [])
  assert(has_kind(kinds, tk_str_interp_end()))
  0


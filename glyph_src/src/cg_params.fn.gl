cg_params mir_fn i =
  pc = glyph_array_len(mir_fn.fn_params)
  match i >= pc
    true -> ""
    _ ->
      param_id = mir_fn.fn_params[i]
      decl = s2("long long ", cg_local(param_id))
      match i == pc - 1
        true -> decl
        _ -> s2(decl, ", ")
